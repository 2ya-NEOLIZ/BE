spring:
  application:
    name: neoliz
  datasource:
    url: jdbc:mysql://localhost:3306/neoliz?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: neoliz
    password: neoliz1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
